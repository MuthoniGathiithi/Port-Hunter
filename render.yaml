
services:
  - type: web
    name: identifacey-face
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn face_service:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8